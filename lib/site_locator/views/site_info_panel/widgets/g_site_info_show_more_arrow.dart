import 'package:flutter/material.dart';

class SiteInfoShowMoreArrow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.expand_more_outlined,
        size: 24,
      ),
    );
  }
}
